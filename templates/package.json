{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "author": "{{ author }}",
  "scripts": {
    "dev": "gulp --silent",
    "prod": "gulp --silent -p",
    "test": "jest"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "balm": "^2.10.0",
    "eslint": "^6.8.0",
    "gulp": "^4.0.2",
    "jest": "^25.1.0"
  },
  "private": true
}
