{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "author": "{{ author }}",
  "scripts": {
    "dev": "gulp --silent",
    "prod": "gulp --silent -p",
    "test": "jest"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "balm": "^2.17.1",
    "eslint": "^7.2.0",
    "gulp": "^4.0.2",
    "jest": "^26.0.1"
  },
  "private": true
}
